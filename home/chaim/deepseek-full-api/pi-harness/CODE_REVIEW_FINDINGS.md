 Full Code Review: XML Tool Call Parsing**Reviewed files:** `src/xml-toolcall-parser.ts` (247 lines) `src/tools.ts` (206 lines) `src/tool-registry.ts` (566 lines) `src/types.ts` (~15 lines) `src/xml-parser-test.ts` + `test-xml.ts` (tests)---## 1. `xml-toolcall-parser.ts` — Critical Issues### Bug: Race condition in `ensureBatchParser()`The `ensureBatchParser()` function checks `batchReady` before `batchInitPromise`, creating a TOCTOU gap. If two callers race:```typescript// current code (pseudocode)if (batchReady) return Promise.resolve();  // caller A sees falseif (batchInitPromise) return batchInitPromise; // caller B sets this between checksbatchInitPromise = batchParser.init().then(() => { batchReady = true; }); // A also sets it```**Severity: High**. Should check `batchInitPromise` first or use a mutex pattern.### Design: WASM dependency for XML parsingThe parser requires `init()` to load a WASM module (SHA3), meaning it cannot parse synchronously. This adds startup latency and a failure mode if WASM fails to load.### Design: Singleton anti-pattern for testing```typescriptexport const batchParser: XmlToolCallParser = new XmlToolCallParser();```Module-level singleton makes testing impossible without resetting state. Should use a factory or dependency injection.---## 2. `tools.ts` — Critical Issues### Bug: `maxBuffer: 1024 * 1024` (1MB hard limit)```typescriptconst result = await execAsync(String(args.command), { cwd: args.cwd || process.cwd(), maxBuffer: 1024 * 1024  // silently truncates output > 1MB});```Large tool outputs are silently truncated. Should either increase the buffer, stream, or explicitly report truncation.### Bug: `summarizeToolOutput()` likely lossyThe output of bash/ls commands goes through `summarizeToolOutput()`, which based on the tool-registry.ts patterns, aggressively compresses output (removes wrapper tags, collapses whitespace). This means the LLM may not see the full tool output — a correctness risk.### Anti-pattern: Scattered error handling (33 error sites)With 33 error sites in 206 lines, error handling is duplicated extensively. Consolidating to a wrapper function would reduce the error surface and prevent drift.### Bug: `args.command` not sanitized before String coercion```typescriptconst result = await execAsync(String(args.command), ...)```If `args.command` is an object, `String()` produces `[object Object]` which gets executed as a shell command. Should validate the type explicitly.---## 3. `tool-registry.ts` — Critical Issues### Security: Regex injection via tool names```typescriptconst toolNames = toolDefs.map((t) => t.name).join('|');result = result.replace(new RegExp(`(${toolNames})>[\\s\\S]*?\\/\\1>`, 'g'), '');```Tool names are interpolated directly into a regex without escaping. A tool name containing regex metacharacters (e.g., `read|write`, or `.*`) would cause incorrect matches or ReDoS (catastrophic backtracking).  **Severity: High**. Must escape tool names with `escapeRegExp()`.### Maintainability: 9 separate `replace()` calls for wrapper tag removal```typescriptresult = result.replace(/