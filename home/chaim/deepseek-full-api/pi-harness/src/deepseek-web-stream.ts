/*** Stream processor for the DeepSeek web chat endpoint.* Parses the proprietary text-based stream format where tool calls* appear as inline markers followed by JSON.** Format:*   Regular text appears as plain lines.*   Tool calls appear as:*     工具调用*     {"name": "...", "arguments": {...}}** Includes SSE timeout handling — resets a 30s timer on each chunk,* aborts the stream with a 'timeout' stop reason if exceeded.*/import { StreamCallbacks, StopReason } from './types';export class DeepSeekWebStream {private callbacks: StreamCallbacks;private buffer = '';private sseTimer: ReturnType | null = null;private readonly SSE_TIMEOUT_MS = 30_000;private finished = false;constructor(callbacks: StreamCallbacks) {this.callbacks = callbacks;}/** Process a single SSE data line. */processLine(line: string): void {if (this.finished) return;this.resetSseTimer();const data = line.startsWith('data: ') ? line.slice(6) : line;if (data === '[DONE]') {this.finish('stop');return;}// Accumulate raw textthis.buffer += data;this.processBuffer();}/** Parse accumulated buffer for text and tool-call markers. */private processBuffer(): void {const toolCallMarker = '工具调用';while (true) {const markerIdx = this.buffer.indexOf(toolCallMarker);if (markerIdx === -1) {// Emit remaining text that's safe (no partial marker possible)const safeEnd = Math.max(0, this.buffer.length - toolCallMarker.length + 1);if (safeEnd > 0) {const text = this.buffer.slice(0, safeEnd);if (text.trim()) {this.callbacks.onText(text);}this.buffer = this.buffer.slice(safeEnd);}return;}// Emit text before the markerif (markerIdx > 0) {const text = this.buffer.slice(0, markerIdx);if (text.trim()) {this.callbacks.onText(text);}}// Try to parse tool call JSON after the markerconst afterMarker = this.buffer.slice(markerIdx + toolCallMarker.length).trimStart();// Find the JSON objectconst jsonStart = afterMarker.indexOf('{');if (jsonStart === -1) {// No JSON yet; consume the marker and continuethis.buffer = afterMarker;continue;}// Try to extract a complete JSON objectconst jsonCandidate = afterMarker.slice(jsonStart);const extracted = this.extractJsonObject(jsonCandidate);if (!extracted) {// JSON is incomplete; wait for more datathis.buffer = afterMarker;return;}try {const parsed = JSON.parse(extracted);if (parsed && typeof parsed.name === 'string') {this.callbacks.onToolCall(parsed.name, parsed.arguments ?? parsed.args ?? {});}} catch {// Invalid JSON — skip and continue}// Consume the marker + parsed JSON from bufferconst consumed = markerIdx + toolCallMarker.length + jsonStart + extracted.length;this.buffer = this.buffer.slice(consumed).trimStart();}}/** Try to extract a balanced JSON object from the start of a string. */private extractJsonObject(str: string): string | null {if (!str.startsWith('{')) return null;let depth = 0;let inString = false;let escaped = false;for (let i = 0; i  str.length; i++) {const ch = str[i];if (escaped) {escaped = false;continue;}if (inString) {if (ch === '\\') escaped = true;else if (ch === '"') inString = false;continue;}if (ch === '"') {inString = true;continue;}if (ch === '{') depth++;else if (ch === '}') {depth--;if (depth === 0) {return str.slice(0, i + 1);}}}return null; // Unbalanced — need more data}private resetSseTimer(): void {if (this.sseTimer) clearTimeout(this.sseTimer);this.sseTimer = setTimeout(() => {this.finish('timeout' as StopReason);}, this.SSE_TIMEOUT_MS);}private finish(reason: StopReason): void {if (this.finished) return;this.finished = true;if (this.sseTimer) {clearTimeout(this.sseTimer);this.sseTimer = null;}this.callbacks.onStopReason?.(reason);this.callbacks.onComplete();}}