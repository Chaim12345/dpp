/*** Stream processor for DeepSeek API native tool-call format.* Handles the streaming delta events where tool_calls appear as* structured JSON in the delta.tool_calls array.** Includes SSE timeout handling — resets a 30s timer on each chunk,* aborts the stream with a 'timeout' stop reason if exceeded.*/import { StreamCallbacks, StopReason } from './types';export class DeepSeekNativeStream {private callbacks: StreamCallbacks;private sseTimer: ReturnType | null = null;private readonly SSE_TIMEOUT_MS = 30_000;private finished = false;constructor(callbacks: StreamCallbacks) {this.callbacks = callbacks;}/** Process a single SSE data line. */processLine(line: string): void {if (this.finished) return;// Reset timeout on every data chunkthis.resetSseTimer();const data = line.startsWith('data: ') ? line.slice(6) : line;if (data === '[DONE]') {this.finish('stop');return;}try {const parsed = JSON.parse(data);this.handleChunk(parsed);} catch {// Ignore unparseable lines (comments, keep-alive pings, etc.)}}/** Handle a parsed SSE chunk object. */private handleChunk(chunk: Record): void {const choices = chunk.choices as Array> | undefined;if (!choices || choices.length === 0) return;const choice = choices[0];// Check finish reason firstconst finishReason = choice.finish_reason as string | undefined;if (finishReason) {this.finish(finishReason as StopReason);return;}const delta = choice.delta as Record | undefined;if (!delta) return;// Extract text contentif (typeof delta.content === 'string' && delta.content.length > 0) {this.callbacks.onText(delta.content);}// Extract native tool calls from deltaconst toolCalls = delta.tool_calls as Array> | undefined;if (toolCalls) {for (const tc of toolCalls) {const fn = tc.function as Record | undefined;if (fn && typeof fn.name === 'string') {let args: Record = {};if (typeof fn.arguments === 'string') {try {args = JSON.parse(fn.arguments);} catch {// Use empty args if JSON is malformed}}this.callbacks.onToolCall(fn.name, args);}}}}private resetSseTimer(): void {if (this.sseTimer) clearTimeout(this.sseTimer);this.sseTimer = setTimeout(() => {this.finish('timeout' as StopReason);}, this.SSE_TIMEOUT_MS);}private finish(reason: StopReason): void {if (this.finished) return;this.finished = true;if (this.sseTimer) {clearTimeout(this.sseTimer);this.sseTimer = null;}this.callbacks.onStopReason?.(reason);this.callbacks.onComplete();}}